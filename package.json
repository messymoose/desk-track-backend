{
  "name": "desk-track-backend",
  "version": "2.1.0",
  "description": "DESK/TRACK backend — syncs contacts, targets, templates across devices and proxies job feeds. Zero dependencies.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": "22.x"
  }
}
